<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/carSelector"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context=".CarSelection"
                android:background="@drawable/bg">

    <TextView
            android:id="@+id/carSelectorText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Select the car"
            android:layout_centerHorizontal="true"
            android:layout_alignParentTop="true"/>

    <Spinner
            android:id="@+id/carManufacturerSpinner"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"/>

    <ListView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/carManufacturerSpinner"
            android:layout_alignParentLeft="true"
            android:id="@+id/carListView"
            android:focusableInTouchMode="true"/>

</RelativeLayout>